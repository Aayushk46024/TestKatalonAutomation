# Gradle - Comment test
# Build your Java project and run tests with Gradle using a Gradle wrapper script.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/java

pool:
  vmImage: 'windows-latest'

steps: 
- task: katalonTask@1
  inputs:
    version: '5.10.1'
    executeArgs: '-projectPath="/Katalon Studio/DemoProject_01/DemoProject_01.prj" -retry=0 -testSuitePath="Test Suites/Registration TestSuite" -browserType="Chrome" -executionProfile="default" -apiKey="c2a52371-9b2d-4d60-8431-4f5478a7fcc8" --config -proxy.auth.option=NO_PROXY -proxy.system.option=NO_PROXY -proxy.system.applyToDesiredCapabilities=true'

- task: PublishTestResults@2
  inputs:
    testResultsFormat: 'JUnit'
    testResultsFiles: '**/JUnit_Report.xml'
    mergeTestResults: true

- task: PublishBuildArtifacts@1
  inputs:
    ArtifactName: 'Reports'
    PathtoPublish: './C:/Users/AayushKhandelwal/Katalon Studio/DemoProject_01/Reports/'